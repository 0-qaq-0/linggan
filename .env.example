# LINGGAN 环境变量示例
#
# 用法：把本文件复制为项目根目录下的 .env，然后按需修改。
#   cp .env.example .env      （Windows: copy .env.example .env）
#
# 注意：.env 已被 .gitignore 忽略，不会被提交；本文件（.env.example）会提交，
# 所以不要把真实密钥写在这里。

# ── 服务端端口 ──
PORT=3001

# ── JWT 签名密钥（必填，服务端启动时会校验）──
#
# 这是登录令牌的签名密钥。泄露它等于任何人都能伪造任意用户的登录状态
# （包括管理员），所以：
#   · 不要使用示例值，也不要使用任何写在文档/仓库里的字符串
#   · 长度至少 16 个字符，建议 32 字节随机数
#
# 生成方式（任选其一）：
#   openssl rand -hex 32
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 服务端启动时会检查：缺失 / 等于历史默认值 / 长度不足 → 直接退出并提示原因。
JWT_SECRET=

# 登录令牌有效期，默认 7 天
JWT_EXPIRES_IN=7d

# ── 首次启动时自动创建的管理员账号（可选）──
# 留空则跳过创建；已有同名账号时不会覆盖。
ADMIN_EMAIL=
ADMIN_PASSWORD=
